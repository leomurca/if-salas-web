const cardContainer = {
  width: '100%',
  padding: '20px',
};

const typographyName = {
  fontWeight: 400,
};

const typographyCreatedAt = {
  fontWeight: 400,
  fontSize: '12px',
};

const textContainer = {
  marginTop: '20px',
};

const styles = {
  cardContainer,
  typographyName,
  typographyCreatedAt,
  textContainer,
};

export { styles };
