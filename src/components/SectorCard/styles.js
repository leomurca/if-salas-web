const card = {
  maxWidth: 510,
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'space-between',
};

const cardMedia = {
  component: 'img',
  height: '140',
};
const styles = { card, cardMedia };
export default styles;
